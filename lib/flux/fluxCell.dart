import 'package:flutter/material.dart';
import '../API/API.dart';
import '../API/Routes.dart';

class FluxCell extends StatelessWidget {
  const FluxCell(this.data);

  final Map<String, dynamic> data;

  @override
  Widget build(BuildContext context) {
    return Text('ok');
  }
}
